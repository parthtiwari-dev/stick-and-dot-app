# Supabase Dashboard > Project Settings > API Keys / Connect
# Example: https://your-project-ref.supabase.co
NEXT_PUBLIC_SUPABASE_URL=

# Public browser-safe key. Prefer sb_publishable_..., legacy anon key also works.
NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY=

# Server-only elevated key. Required only for `npm run seed:dev`.
# Use Supabase secret key or legacy service_role key. Never expose this to the browser.
SUPABASE_SERVICE_ROLE_KEY=
# Alternative name supported by the seed script:
# SUPABASE_SECRET_KEY=

# Dev-only role switcher. Keep disabled in production.
DEV_AUTH_ENABLED=True
NEXT_PUBLIC_DEV_AUTH_ENABLED=True

# Controls what happens when a writer submits an article.
# before_publish | commissions_only | optional
SME_REVIEW_MODE=before_publish
NEXT_PUBLIC_SME_REVIEW_MODE=before_publish

# Seeded test account defaults used by `npm run seed:dev` and dev login.
DEV_TEST_PASSWORD=StickDotDev123!
DEV_TEST_DEV_WRITER_EMAIL=dev-writer@stickanddot.test
DEV_TEST_WRITER_EMAIL=writer@stickanddot.test
DEV_TEST_SME_EMAIL=sme@stickanddot.test
DEV_TEST_READER_EMAIL=reader@stickanddot.test
DEV_TEST_BUSINESS_EMAIL=business@stickanddot.test
